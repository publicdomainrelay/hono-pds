{
  "name": "hono-pds",
  "description": "AT Protocol PDS server",
  "options": {
    "port": {
      "type": "number",
      "description": "Port to listen on",
      "env": "PORT",
      "default": 2583
    },
    "hostname": {
      "type": "string",
      "description": "Hostname to bind to",
      "env": "HOSTNAME",
      "default": "127.0.0.1"
    }
  }
}
